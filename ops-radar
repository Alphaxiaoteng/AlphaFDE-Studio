#!/usr/bin/env bash
# ==============================================================================
# ops-radar: 云谷中心政策与活动雷达 CLI 快捷指令包装
# ==============================================================================
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec /usr/bin/env python3 "$DIR/policy_radar_cli.py" "$@"
