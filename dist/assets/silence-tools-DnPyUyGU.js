var e={thresholdDb:-26,minSilenceMs:600,padMs:150},t=[{name:`remove_silence`,description:[`Remove dead air — quiet, speech-free stretches — from clips, ripple-closing each gap on its own track (ONE undoable batch).`,`Detection is on-device and relative: a stretch counts as silence only when its level sits well below the clip's own speech level`,`(so music beds and loud ambience are never cut), it lasts at least minSilenceMs, and a padMs breathing room is kept on both sides.`,`Use this to tighten pacing (long pauses, dead space between takes). It complements word-level editing:`,`transcribed clips that already have word edits or gap caps are skipped — use clean_script there, it trims pauses word-precisely.`,`Clips with playbackRate≠1 or an animated zoom are skipped (reported in skipped[]). Ripple is per-track: other tracks do not shift.`,`Call once with NO itemId to sweep every audio/video clip on the active timeline; pass itemId for a single clip.`,`Pass dryRun:true to preview the cut list (seconds) without editing.`].join(` `),input_schema:{type:`object`,properties:{itemId:{type:`string`,description:`Only this clip (prefix id ok). Omit to process every audio/video clip.`},thresholdDb:{type:`number`,minimum:-60,maximum:-6,description:`Silence gate relative to the clip's speech level in dB (default ${e.thresholdDb}; more negative = more conservative).`},minSilenceMs:{type:`number`,minimum:200,maximum:1e4,description:`Only remove pauses at least this long (default ${e.minSilenceMs}ms).`},padMs:{type:`number`,minimum:0,maximum:1e3,description:`Breathing room kept on each side of a cut (default ${e.padMs}ms).`},dryRun:{type:`boolean`,description:`true = report the would-be cuts without editing.`}}}}],n=new Set(t.map(e=>e.name));export{t as n,n as t};